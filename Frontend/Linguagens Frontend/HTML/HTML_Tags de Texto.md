# 📌 HTML - Tags de Texto

As tags de texto no HTML são utilizadas para estruturar e formatar conteúdos escritos dentro da página.  
Elas ajudam a definir **parágrafos**, **ênfases**, **quebras de linha** e **destaques** de modo semântico.

---

## **1) `p` — Parágrafo**
Representa um **bloco de texto** organizado como um parágrafo.  
É utilizado para criar textos corridos e estruturados.

---

## **2) `span` — Texto Inline**
Usado para **marcar** ou **destacar** uma parte específica do texto **sem quebrar a linha**.  
É útil para aplicar estilos em palavras ou trechos menores.

---

## **3) `strong` — Destaque Importante**
Indica que uma parte do texto possui **importância semântica**.  
Normalmente aparece como **negrito**, mas o principal é o significado, não o estilo.

---

## **4) `em` — Ênfase**
Indica que o texto deve ter **ênfase**.  
Geralmente aparece em **itálico**, porém o importante é que destaca algo relevante na leitura.

---

## **5) `br` — Quebra de Linha**
Cria uma **quebra de linha direta** dentro do texto.  
Usado quando precisa mudar de linha **sem iniciar um novo parágrafo**.

---

## **6) `hr` — Linha Horizontal**
Representa uma **divisão de seção**, criando um traço horizontal.  
Usado para separar blocos de conteúdo ou indicar mudança de assunto.

---

## 🏁 Resumo

| Tag | Tipo | Função |
|-----|------|--------|
| `p` | Bloco | Criar parágrafos |
| `span` | Inline | Destacar partes pequenas do texto |
| `strong` | Inline | Dar importância ao conteúdo |
| `em` | Inline | Marcar texto com ênfase |
| `br` | Inline | Quebrar linha |
| `hr` | Bloco | Separar seções |

