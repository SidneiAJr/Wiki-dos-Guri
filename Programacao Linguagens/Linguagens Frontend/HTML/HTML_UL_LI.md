# 📌 Documentação: Elementos `ul` e `li` no HTML

Os elementos **`ul`** e **`li`** são utilizados para criar **listas** no HTML.

---

## **1) `ul` — Lista Não Ordenada**

O elemento **`ul`** representa uma **lista sem ordem numérica**.  
Os itens são exibidos geralmente com **marcadores** (pontos, círculos ou símbolos).

Usamos `ul` quando a **sequência dos itens não é importante**.

Exemplo de uso típico:
- Listas de tópicos
- Características
- Menus simples
- Agrupamento de informações sem prioridade

---

## **2) `li` — Item da Lista**

O elemento **`li`** representa **cada item** individual dentro de uma lista.  
Ele só pode existir **dentro de listas**, como `ul` ou `ol`.

Cada `li` é um **elemento independente**, podendo conter:
- texto,
- links,
- imagens,
- ou até outros elementos dentro dele.

---

## **Relação Entre Eles**

| Elemento | Função |
|---------|--------|
| `ul` | Cria a lista |
| `li` | Representa cada item dentro da lista |

Sem `li`, a lista não tem conteúdo.  
Sem `ul`, os itens não têm contexto de lista.

---

## 🧠 Quando Usar

Use **`ul` + `li`** quando:
- A ordem **não importa**
- Você está mostrando informações em **tópicos**
- Está criando **menus ou navegações**

Se a ordem **for importante**, aí usamos **`ol`** (lista ordenada).

---

## ✅ Resumo Final

| Elemento | Tipo de Lista | Indicado para |
|---------|----------------|---------------|
| `ul` | Não ordenada | Listas gerais, menus e tópicos |
| `li` | Itens da lista | Conteúdo dentro de qualquer lista |

---

