# 📌 Documentação: Tipos de Input no HTML

O elemento **`input`** no HTML é utilizado para receber dados do usuário.  
Ele pode assumir diferentes **tipos**, cada um destinado a uma forma específica de entrada.

Abaixo estão os principais tipos e suas finalidades:

---

## **1) text**
Recebe texto comum em uma única linha. Utilizado para informações gerais, como nomes ou títulos.

---

## **2) password**
Recebe texto, mas oculta visualmente os caracteres. Utilizado para senhas e informações sensíveis.

---

## **3) email**
Usado para entrada de endereços de e-mail. Permite validações básicas de formato.

---

## **4) number**
Aceita apenas valores numéricos. Pode permitir configurações de limites mínimos e máximos.

---

## **5) tel**
Usado para números de telefone. Não valida formato, mas orienta dispositivos móveis a exibirem teclado numérico.

---

## **6) url**
Destinado a endereços de internet. Realiza validação básica de estrutura.

---

## **7) date**
Permite selecionar datas utilizando um calendário.

---

## **8) time**
Utilizado para seleção de horários (horas e minutos).

---

## **9) datetime-local**
Combina data e hora na mesma seleção, sem fuso horário.

---

## **10) month**
Permite selecionar apenas mês e ano.

---

## **11) week**
Permite selecionar a semana específica de um ano.

---

## **12) checkbox**
Permite marcar ou desmarcar uma opção. Pode ser usado sozinho ou em grupo.

---

## **13) radio**
Usado para selecionar **uma** opção dentro de um grupo mutuamente exclusivo.

---

## **14) range**
Permite escolher um valor dentro de um intervalo, utilizando um controle deslizante.

---

## **15) file**
Permite selecionar e enviar arquivos a partir do dispositivo do usuário.

---

## **16) color**
Abre um seletor de cores para entrada de valores representando cores.

---

## **17) hidden**
Mantém um valor guardado no formulário sem exibição visual para o usuário.

---

## **18) submit**
Envia os dados preenchidos no formulário.

---

## **19) reset**
Restaura todos os valores do formulário aos estados iniciais.

---

## **20) button**
Cria um botão genérico que pode ser associado a ações personalizadas via JavaScript.

---

# ✅ Resumo

| Tipo | Finalidade |
|------|-----------|
| `text` | Texto simples |
| `password` | Texto oculto |
| `email`, `url`, `tel` | Dados com formato específico |
| `number`, `range` | Valores numéricos |
| `date`, `time`, `datetime-local`, `month`, `week` | Seleção de datas e horários |
| `file` | Envio de arquivos |
| `checkbox`, `radio` | Seleção de opções |
| `color` | Seleção de cores |
| `hidden` | Valores invisíveis no formulário |
| `submit`, `reset`, `button` | Botões de ação |

---

