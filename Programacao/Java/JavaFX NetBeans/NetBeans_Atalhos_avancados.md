# ⚡️ Atalhos de Código Avançados no NetBeans

Esses são **atalhos menos conhecidos**, mas extremamente úteis no dia a dia para escrever código Java de forma rápida e consistente.

## 🧩 Templates e Expansões de Código

| Atalho | Expansão | Descrição |
|:--------|:-----------|:-----------|
| **itar** | `for (int i = 0; i < array.length; i++) { ... }` | Cria um loop que itera automaticamente sobre um array. |
| **fore** | `for (Tipo item : colecao) { ... }` | Igual ao `foreach`, mas mais curto. |
| **rthr** | `throw new RuntimeException("");` | Lança uma exceção `RuntimeException`. |
| **pf** | `private final Tipo variavel;` | Cria um atributo `private final`. |
| **psf** | `public static final Tipo NOME = valor;` | Cria uma constante pública e estática. |
| **psvm + Tab** | `public static void main(String[] args) { }` | Gera automaticamente o método principal `main()`. |
| **serr** | `System.err.println();` | Imprime mensagens de erro no console. |
| **st** | `String texto = "";` | Declara rapidamente uma variável `String`. |
| **sb** | `StringBuilder sb = new StringBuilder();` | Cria um `StringBuilder` para concatenação eficiente. |
| **cs** | `case : break;` | Adiciona um caso padrão dentro de um `switch`. |
| **doit** | `do { } while ();` | Cria um laço `do-while`. |
| **wh** | `while () { }` | Cria um laço `while` simples. |
| **ifn** | `if (variavel == null) { }` | Verifica se a variável é nula. |
| **inn** | `if (variavel != null) { }` | Verifica se a variável *não* é nula. |
| **null** | `= null;` | Atribui `null` a uma variável. |
| **prsf** | `private static final Tipo NOME = valor;` | Cria uma constante privada e estática. |

---

💡 **Dica:**  
Você pode visualizar e criar seus próprios atalhos personalizados indo em:
