# 🧠 Lógica de Programação & Programação Orientada a Objetos (POO)

Este documento tem como objetivo explicar os conceitos fundamentais de **lógica de programação** e como eles evoluem naturalmente para o paradigma de **Programação Orientada a Objetos (POO)**.

---

## 1) Lógica de Programação

Lógica de programação é o processo de organizar o raciocínio para resolver um problema de forma estruturada.

### Conceitos Básicos

| Conceito | Descrição |
|--------|----------|
| **Entrada** | Dados que o programa recebe (ex.: teclado, arquivo, sensor). |
| **Processamento** | Operações realizadas sobre os dados (ex.: cálculos, comparações). |
| **Saída** | Resultado final exibido (ex.: texto, gráfico, resposta). |

### Estruturas Fundamentais

| Estrutura | Objetivo |
|---------|----------|
| **Sequência** | Execução passo a passo. |
| **Decisão (IF/ELSE)** | Escolher caminhos diferentes conforme uma condição. |
| **Repetição (LOOP)** | Repetir algo até uma condição ser atendida. |

### Variáveis e Tipos

Variáveis são espaços na memória para armazenar valores.

Tipos comuns:
- Numéricos (inteiro, decimal)
- Texto (string)
- Lógico (booleano: verdadeiro/falso)

### Algoritmo

Um algoritmo é um conjunto de passos para resolver um problema de forma lógica e finita.

> Pensar em algoritmo é pensar em **como resolver**, não ainda *como escrever no código*.

---

## 2) Evolução da Lógica para POO

Quando sistemas se tornam maiores, a lógica procedural (passo a passo) começa a ficar difícil de organizar.  
A POO surge para **organizar o código como se fosse composto por objetos** que representam coisas do mundo real.

---

## 3) O que é POO?

POO (Programação Orientada a Objetos) é um modelo de desenvolvimento baseado em **objetos**.

Um objeto tem:

| Elemento | Significado |
|---------|-------------|
| **Atributos** | Características (dados) |
| **Métodos** | Comportamentos (ações) |

Exemplo mental:  
Um **Carro** tem *cor, velocidade, marca* (atributos)  
e pode *acelerar, frear* (métodos).

---

## 4) Classe e Objeto

| Termo | Explicação |
|------|------------|
| **Classe** | Modelo, molde, definição de algo. |
| **Objeto** | Instância da classe, algo concreto criado a partir dela. |

> Classe é a ideia.  
> Objeto é a coisa real que existe na memória.

---

## 5) Pilares da POO

| Pilar | Objetivo | Explicação |
|------|----------|------------|
| **Abstração** | Simplificar | Representar apenas o que importa. |
| **Encapsulamento** | Proteger dados | Controlar o acesso através de métodos. |
| **Herança** | Reutilizar código | Uma classe pode herdar características de outra. |
| **Polimorfismo** | Comportamentos diferentes | Um mesmo método pode agir de formas diferentes dependendo do contexto. |

---

## 6) Vantagens da POO

- Organização do código
- Reutilização de componentes
- Facilidade de manutenção
- Melhor legibilidade
- Escalabilidade em grandes projetos

---

## 7) Quando usar POO?

- Projetos grandes
- Sistemas que possuem múltiplas entidades/objetos
- Programas que precisam crescer com novas funções com o tempo

---

## 8) Resumo Visual

