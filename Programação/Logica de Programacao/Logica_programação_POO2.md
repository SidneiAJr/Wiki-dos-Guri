# 🧠 Lógica de Programação | POO (Programação Orientada a Objetos)

## O que é POO?

POO é um paradigma de programação que organiza o sistema em **objetos**.

Esses objetos representam coisas do mundo real e possuem:
- Dados (atributos)
- Comportamentos (métodos)

O objetivo da POO é:
- Organização
- Reutilização
- Facilidade de manutenção
- Código mais limpo

---

## 🧱 O que é Classe?

Uma classe é um **modelo** ou **molde**.

Ela define:
- Quais dados um objeto terá
- Quais ações esse objeto poderá executar

A classe **não é o objeto**, ela é apenas a definição dele.

---

## 🧍 O que é Objeto?

Objeto é uma **instância de uma classe**.

É a classe em uso, na prática.

Uma classe pode gerar vários objetos diferentes.

---

## 🔌 O que é Interface?

Interface é um **contrato**.

Ela define:
- Quais métodos devem existir
- O que uma classe é obrigada a implementar

A interface **não diz como fazer**, apenas **o que deve ser feito**.

Ela é usada para padronizar comportamentos.

---

## 🧩 O que é Classe Abstrata?

Classe abstrata é uma classe **incompleta**.

Ela:
- Não pode ser instanciada
- Serve como base para outras classes
- Pode ter métodos prontos e métodos incompletos

É usada quando várias classes compartilham algo em comum.

---

## 🔁 Polimorfismo

Polimorfismo significa:
> **Um mesmo comportamento com várias formas.**

Objetos diferentes podem responder de forma diferente ao mesmo método.

Isso permite código mais flexível e desacoplado.

---

## 🔐 Getters & Setters

Getters e Setters são métodos usados para:
- Controlar o acesso aos dados
- Proteger os atributos
- Validar valores antes de alterar

Eles evitam acesso direto às variáveis.

---

## 🔧 Métodos

Métodos são as **ações** que um objeto pode executar.

Eles representam o comportamento da classe.

---

## 📌 Tipos de Métodos

### Método sem retorno (`void`)

Executa uma ação, mas não devolve valor.

---

### Método com retorno

Executa uma ação e devolve um valor ao final.

O retorno pode ser:
- Número
- Texto
- Objeto
- Booleano

---

### Método estático (`static`)

Pertence à classe, não ao objeto.

Pode ser usado sem criar uma instância.

É comum para:
- Utilidades
- Funções auxiliares
- Métodos globais

---

## 🧠 Resumo Final

- Classe define
- Objeto executa
- Interface obriga
- Classe abstrata guia
- Polimorfismo flexibiliza
- Getter/Setter protege
- Métodos executam ações
